name: cyprus_travel_app
description: "دليل قبرص السياحي - تطبيق سفر متكامل بعدة لغات"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  firebase_core: ^3.6.0
  firebase_database: ^11.1.4

  google_maps_flutter: ^2.9.0

  permission_handler: ^11.3.1

  url_launcher: ^6.3.1

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
